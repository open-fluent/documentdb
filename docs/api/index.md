# API Reference

This section is generated from the public XML documentation and metadata of the Fluent.DocumentDb packages.

Start with the generated namespace and type pages in the table of contents, or use search to find a specific contract such as `IDocumentStore`, `IEventStore`, or `AddDocumentDbEventStore`.

> The library is in early development. Always verify exact signatures against the API reference for the package version you install.
